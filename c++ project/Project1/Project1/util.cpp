/*
#include "util.h"

float rand0to1()
{
	return rand() / (float)RAND_MAX;
}
*/
#include "util.h"

float rand0to1()
{
	return rand() / (float)RAND_MAX;
}

float rand0_1to1() {
	return rand() / (float)RAND_MAX;
}